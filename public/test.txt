Task:
Extract all questions from the attached PDF and convert them into a valid JSON array using the strict schema provided below.
All mathematical expressions must be accurately preserved and converted into inline LaTeX, wrapped in $...$.

Output Instructions:
Divide the total output into 3 equal parts (if possible).

After each part, wait for my prompt “next” before continuing.

Only output a valid JSON array — no extra text, markdown, comments, or formatting outside JSON.

Ensure each JSON part is fully parseable with no trailing commas or formatting issues.

 LaTeX Formatting Rules (Strict):
Wrap all math in $...$.

Fix broken/incomplete expressions using valid LaTeX.

Common examples:

Integrals: $\int x^2 , dx$

Fractions: $\frac{a}{b}$

Matrices: $\begin{bmatrix} a & b \ c & d \end{bmatrix}$

Limits/Sums/Products: $\lim_{x \to \infty}$, $\sum_{i=1}^{n}$, $\prod_{i=1}^{n}$

Roots: $\sqrt{x}$, $\sqrt[n]{x}$

Complex Numbers: $z = x + iy$, $\arg(z)$

Vectors: $\vec{A}$, $\hat{i}$, $|\vec{F}|$

Greek letters & symbols: $\theta$, $\alpha$, $\pi$, etc.

Escape special characters: _ as \_, % as \%

Use \left and \right for scalable brackets:
e.g., $\left| \arg\left( \frac{z - 1}{z + 1} \right) \right|$

JSON Output Format (Strict):

[
  {
    "type": "MCQ" | "MSQ" | "Numerical" | "Descriptive",
    "question": {
      "text": "Full question text with valid LaTeX (math in $...$).",
      "imageUrl": ""
    },
    "options": {
      "option1": { "text": "", "imageUrl": "" },
      "option2": { "text": "", "imageUrl": "" },
      "option3": { "text": "", "imageUrl": "" },
      "option4": { "text": "", "imageUrl": "" }
    },
    "correctOption": ["optionX"],
    "answer": "",
    "explanation": "",
    "publication": "",
    "subject": "",
    "topic": "",
    "level": "Easy" | "Medium" | "Hard",
    "unit": "",
    "error": "",
    "createdBy": "",
    "askedIn": {
      "exam": "",
      "year": 2020,
      "date": "",
      "marks": 3
    }
  }
]
Important Rules:
Remove any labels from options like (a), (b), 1., 2., etc.
Example: "text": "2"  instead of "text": "(b) 2" 

Set "correctOption": [] and "answer": "" if the correct option is unknown.

Extract and place image URLs if present, else keep "imageUrl": "".

Always keep "createdBy": "".

Final JSON must be 100% valid, clean, and parseable — no extra characters or errors.

Strictly follow this structure — do not add or remove any fields.

